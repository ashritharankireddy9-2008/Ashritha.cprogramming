#include<stdio.h>
int main()
{
	//l is length ,b is breadth
	int l,b;
	float area;
	printf("enter l,b values");
	scanf("%d%d",&l,&b);
	area=0.5*l*b;
	printf("the area of triangle %fn",area);
	return 0;
}
